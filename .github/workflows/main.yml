name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
           login-server: knowledgefactoryregistery.azurecr.io
           username: KnowledgefactoryRegistery
           password: nF6qVl4ufjhyl=tCSXB+MoBw4Fih40No
      - run: |
          docker build . -t knowledgefactoryregistery.azurecr.io/knowledgefactory:${{ github.sha }}
          docker push knowledgefactoryregistery.azurecr.io/knowledgefactory:${{ github.sha }}
